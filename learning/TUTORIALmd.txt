# 📘 Mini-Tutorial: Building a HIPAA-Compliant Full-Stack Application with Django, React, and Docker

_A student-authored tutorial documenting a key integration or concept learned in Semester 5._

---

## ❓ What This Teaches

**In one paragraph, explain the concept, tool, or integration.**  
What problem does it solve? What kind of project would benefit from using it?

> This tutorial shows how to build a complete HIPAA-compliant healthcare application using Django REST Framework, React, and Docker. It covers database encryption with Fernet, JWT authentication, SSL/TLS implementation, comprehensive audit logging, and secure deployment practices. This solution is essential for any healthcare application handling Protected Health Information (PHI) and demonstrates real-world security implementation for compliance requirements.

---

## 🎯 Use Case

> What real-world need or job scenario does this apply to?

-   [x] Backend development
-   [x] Cybersecurity
-   [x] Monitoring / Observability
-   [ ] Performance / Testing
-   [x] Authentication / Authorization
-   [x] DevOps / Deployments
-   [ ] Other: _Healthcare Compliance Management_

---

## 🚀 Quick Setup / Install

Show just what's needed to get started.

```bash
# Backend dependencies
pip install django djangorestframework djangorestframework-simplejwt cryptography waitress

# Frontend dependencies
npx create-react-app frontend
cd frontend && npm install axios

# Docker setup
docker --version
docker-compose --version

# Optional: Nginx for production
sudo apt-get install nginx  # Ubuntu/Debian
brew install nginx          # macOS
```

---

## 🛠️ Step-by-Step Guide

Write the steps clearly and briefly. Use bullet points or numbers.

1. **Set up Django backend with encryption:**

    ```python
    # settings.py
    from cryptography.fernet import Fernet
    import os

    # Generate encryption key
    key = Fernet.generate_key()
    with open('encryption.key', 'wb') as f:
        f.write(key)

    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.sqlite3',
            'NAME': 'encrypted_hipaa.db',
        }
    }

    # Security settings
    SECURE_SSL_REDIRECT = True
    SECURE_HSTS_SECONDS = 31536000
    SECURE_HSTS_INCLUDE_SUBDOMAINS = True
    SECURE_HSTS_PRELOAD = True
    ```

2. **Create encrypted models:**

    ```python
    # models.py
    from django.db import models
    from cryptography.fernet import Fernet
    import base64

    class ChecklistItem(models.Model):
        title = models.CharField(max_length=200)
        description = models.TextField()
        status = models.CharField(max_length=20)
        encrypted_data = models.TextField(blank=True)
        
        def encrypt_sensitive_data(self, data):
            with open('encryption.key', 'rb') as f:
                key = f.read()
            f = Fernet(key)
            return f.encrypt(data.encode()).decode()
    ```

3. **Implement JWT authentication:**

    ```python
    # views.py
    from rest_framework_simplejwt.views import TokenObtainPairView
    from rest_framework.decorators import api_view, permission_classes
    from rest_framework.permissions import IsAuthenticated

    @api_view(['GET'])
    @permission_classes([IsAuthenticated])
    def health_check(request):
        return Response({
            "status": "ok",
            "uptime": "2053s",
            "database": "connected",
            "timestamp": timezone.now().isoformat()
        })
    ```

4. **Set up React frontend with secure API calls:**

    ```javascript
    // api.js
    import axios from 'axios';

    const API_BASE_URL = process.env.REACT_APP_API_URL || 'https://localhost:8000';

    const api = axios.create({
      baseURL: API_BASE_URL,
      headers: {
        'Content-Type': 'application/json',
      },
    });

    // Add JWT token to requests
    api.interceptors.request.use((config) => {
      const token = localStorage.getItem('access_token');
      if (token) {
        config.headers.Authorization = `Bearer ${token}`;
      }
      return config;
    });
    ```

5. **Create Docker configuration:**

    ```yaml
    # docker-compose.yml
    version: '3.8'
    services:
      backend:
        build: ./backend
        ports:
          - "8000:8000"
        environment:
          - DEBUG=False
          - SECRET_KEY=your-secret-key
      
      frontend:
        build: ./frontend
        ports:
          - "3000:3000"
        depends_on:
          - backend
      
      nginx:
        image: nginx:alpine
        ports:
          - "443:443"
        volumes:
          - ./nginx-https.conf:/etc/nginx/nginx.conf
          - ./ssl:/etc/ssl/certs
    ```

6. **Deploy with SSL/TLS:**

    ```bash
    # Generate self-signed certificates
    openssl req -x509 -newkey rsa:4096 -keyout ssl/private.key -out ssl/certificate.crt -days 365 -nodes

    # Start the application
    docker-compose up -d
    ```

---

## ✅ What You Should See

Describe the working result — terminal output, UI screenshot, etc.

> **Backend Health Check:**
>
> -   Visiting `https://localhost:8000/api/health/` returns:
>     ```json
>     {
>       "status": "ok",
>       "uptime": "2053s",
>       "version": "1.0.2",
>       "database": "connected",
>       "timestamp": "2025-01-15T14:31:24Z"
>     }
>     ```
>
> **Frontend Dashboard:**
> -   React app loads at `https://localhost:3000`
> -   Login form with JWT authentication
> -   HIPAA compliance checklist interface
> -   Real-time status updates and reporting
>
> **Security Validation:**
> -   SSL certificate shows as valid in browser
> -   Database files have secure permissions (0o600)
> -   JWT tokens expire after configured time
> -   All API endpoints require authentication

You can embed screenshots if helpful:

```markdown
![HIPAA Dashboard Screenshot](../PROOF/hipaa-dashboard.png)
![Security Headers Screenshot](../PROOF/security-headers.png)
![Docker Containers Screenshot](../PROOF/docker-containers.png)
```

---

## 💡 Pro Tips / Edge Cases

List 1–2 important reminders:

-   **Windows PowerShell Issue**: If using Waitress server on Windows, avoid emoji characters in server scripts as they cause encoding errors. Create a separate `waitress_secure_fixed.py` without emojis.
-   **File Permissions**: Always set database file permissions to 0o600 (readable/writable by owner only) for SQLite databases containing sensitive data. Use `os.chmod('database.db', 0o600)` after database creation.
-   **Encryption Key Management**: Store encryption keys securely and never commit them to version control. Consider using environment variables or a key management service in production.
-   **SSL Certificate Warnings**: Self-signed certificates will show browser warnings. For production, use certificates from a trusted Certificate Authority (CA).

---

## 📚 Learn More

Link to any docs or references you found useful:

-   [Django Security Documentation](https://docs.djangoproject.com/en/stable/topics/security/)
-   [Django REST Framework JWT](https://django-rest-framework-simplejwt.readthedocs.io/)
-   [Cryptography Library (Fernet)](https://cryptography.io/en/latest/fernet/)
-   [Docker Compose Documentation](https://docs.docker.com/compose/)
-   [Nginx SSL Configuration](https://nginx.org/en/docs/http/configuring_https_servers.html)
-   [HIPAA Compliance Guidelines](https://www.hhs.gov/hipaa/for-professionals/security/index.html)
-   [OWASP Security Headers](https://owasp.org/www-project-secure-headers/)

---

## 👤 Authored by: [Craig Weinstein]

🗓️ Date: 2025-09-26  
🔁 Validated by: [Educator or Peer Name if applicable]
